<apex:page controller="picklist_exe2">
    <apex:form id="one">
    <apex:selectList size="1" value="{!selected}">
        <apex:selectOptions value="{!options}">
        </apex:selectOptions>
        <apex:actionSupport event="onchange" reRender="one"/>
        </apex:selectList>
        <br/><br/>
        selected value is: {!selected}
    </apex:form>
</apex:page>